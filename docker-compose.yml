services:
  consul-server:
    image: "consul:1.15"
    container_name: consul-server
    ports:
      - "8500:8500"   # UI и HTTP API
      - "8600:8600/udp" # DNS
      - "8300:8300"   # Server RPC
      - "8301:8301"   # Serf LAN
      - "8302:8302"   # Serf WAN
    command: agent -server -ui -node=server-1 -bootstrap-expect=1 -client=0.0.0.0 -bind=0.0.0.0
    volumes:
      - "consul_data:/consul/data"
    networks:
      - bank-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8500/v1/status/leader" ]
      interval: 30s
      timeout: 10s
      retries: 5

  api-gateway-server:
    build:
      context: ./api-gateway-server
      dockerfile: Dockerfile
    container_name: api-gateway-server
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_CLOUD_CONSUL_HOST: consul-server
    depends_on:
      - consul-server
    networks:
      - bank-network

  postgres:
    #    profiles: ["prod"]
    image: "postgres:15"
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    depends_on:
      - consul-server
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - bank-network

  serv-auth-service:
    restart: on-failure
    build:
      context: ./serv-auth-service
      dockerfile: Dockerfile
    container_name: serv-auth-service
    ports:
      - "9000:9000"
    environment:
      SPRING_CLOUD_CONSUL_HOST: consul-server
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres:5432/serv_auth_service
      SPRING_R2DBC_USERNAME: postgres
      SPRING_R2DBC_PASSWORD: postgres
      DB_NAME: auth_db
      DB_HOST: postgres
      DB_PORT: 5432
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    #      SPRING_CLOUD_CONSUL_PORT: 8500
    depends_on:
      - consul-server
      - api-gateway-server
      - postgres
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - bank-network

  account-service:
    build:
      context: ./account-service
      dockerfile: Dockerfile
    container_name: account-service
    ports:
      - "8082:8082"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres:5432/account_service
      DB_NAME: account_service
      DB_HOST: postgres
      DB_PORT: 5432
      SPRING_R2DBC_USERNAME: postgres
      SPRING_R2DBC_PASSWORD: postgres
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - postgres
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8082/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  user-auth-service:
    build:
      context: ./user-auth-service
      dockerfile: Dockerfile
    container_name: user-auth-service
    ports:
      - "8083:8083"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      AUTH_SERVICE_TOKEN_URL: http://api-gateway-server:8080/serv-auth-service/oauth2/token
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
#      - JWT_SECRET=your-256-bit-secret-key-base64-encoded-here-must-be-32-chars
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - account-service
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8083/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  front-ui-service:
    build:
      context: ./front-ui-service
      dockerfile: Dockerfile
    container_name: front-ui-service
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      AUTH_SERVICE_TOKEN_URL: http://api-gateway-server:8080/serv-auth-service/oauth2/token
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - account-service
      - user-auth-service
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8081/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  blocker-service:
    build:
      context: ./blocker-service
      dockerfile: Dockerfile
    container_name: blocker-service
    ports:
      - "8084:8084"
    environment:
      SPRING_CLOUD_CONSUL_HOST: consul-server
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres:5432/blocker_service
      SPRING_R2DBC_USERNAME: postgres
      SPRING_R2DBC_PASSWORD: postgres
      DB_NAME: blocker_service
      DB_HOST: postgres
      DB_PORT: 5432
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - postgres
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8084/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  cash-service:
    build:
      context: ./cash-service
      dockerfile: Dockerfile
    container_name: cash-service
    ports:
      - "8085:8085"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres:5432/cash_service
      SPRING_R2DBC_USERNAME: postgres
      SPRING_R2DBC_PASSWORD: postgres
      DB_NAME: cash_service
      DB_HOST: postgres
      DB_PORT: 5432
      AUTH_SERVICE_TOKEN_URL: http://api-gateway-server:8080/serv-auth-service/oauth2/token
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - postgres
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8085/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  exchange-service:
    build:
      context: ./exchange-service
      dockerfile: Dockerfile
    container_name: exchange-service
    ports:
      - "8086:8086"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres:5432/exchange_service
      SPRING_R2DBC_USERNAME: postgres
      SPRING_R2DBC_PASSWORD: postgres
      DB_NAME: exchange_service
      DB_HOST: postgres
      DB_PORT: 5432
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - postgres
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8086/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  exchange-generator-service:
    build:
      context: ./exchange-generator-service
      dockerfile: Dockerfile
    container_name: exchange-generator-service
    ports:
      - "8087:8087"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      AUTH_SERVICE_TOKEN_URL: http://api-gateway-server:8080/serv-auth-service/oauth2/token
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - exchange-service
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8087/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  transfer-service:
    build:
      context: ./transfer-service
      dockerfile: Dockerfile
    container_name: transfer-service
    ports:
      - "8088:8088"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      SPRING_R2DBC_URL: r2dbc:postgresql://postgres:5432/transfer_service
      SPRING_R2DBC_USERNAME: postgres
      SPRING_R2DBC_PASSWORD: postgres
      DB_NAME: transfer_service
      DB_HOST: postgres
      DB_PORT: 5432
      AUTH_SERVICE_TOKEN_URL: http://api-gateway-server:8080/serv-auth-service/oauth2/token
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
      - postgres
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8088/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

  notification-service:
    build:
      context: ./notification-service
      dockerfile: Dockerfile
    container_name: notification-service
    ports:
      - "8089:8089"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CLOUD_CONSUL_HOST: consul-server
      AUTH_SERVICE_JWT_ISSUER_URI: http://api-gateway-server:8080/serv-auth-service
      AUTH_SERVICE_JWT_JWK_SET_URI: http://api-gateway-server:8080/serv-auth-service/oauth2/jwks
    depends_on:
      - consul-server
      - api-gateway-server
      - serv-auth-service
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8089/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - bank-network

networks:
  bank-network:
    driver: bridge

volumes:
  consul_data:
  postgres_data:
  blocker_data:
  cash_data: