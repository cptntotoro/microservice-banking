spring:
  application:
    name: discovery-server

server:
  port: 8761

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.springframework.cloud.consul: DEBUG
    ru.practicum: DEBUG