#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-user-auth-test-connection"
#  labels:
#    app: user-auth-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['user-auth-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never