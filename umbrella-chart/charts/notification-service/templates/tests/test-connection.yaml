#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-notification-test-connection"
#  labels:
#    app: notification-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['notification-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never