#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-exchange-test-connection"
#  labels:
#    app: exchange-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['exchange-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never