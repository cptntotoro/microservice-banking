#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-front-test-connection"
#  labels:
#    app: front-ui-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['front-ui-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never