#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-test-connection"
#  labels:
#    app: account-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['account-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never