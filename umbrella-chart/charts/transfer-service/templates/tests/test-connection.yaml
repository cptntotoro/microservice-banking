#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-transfer-test-connection"
#  labels:
#    app: transfer-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['transfer-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never