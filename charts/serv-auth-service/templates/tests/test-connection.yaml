#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-test-connection"
#  labels:
#    app: serv-auth-service
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['serv-auth-service:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never