apiVersion: v1
kind: Service
metadata:
  name: serv-auth-service
  labels:
    app: serv-auth-service
spec:
  selector:
    app: serv-auth-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}