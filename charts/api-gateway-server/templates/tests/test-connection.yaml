#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ .Release.Name }}-test-connection"
#  labels:
#    app: api-gateway-server
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['api-gateway-server:{{ .Values.service.port }}/actuator/health']
#  restartPolicy: Never