apiVersion: v1
kind: Service
metadata:
  name: blocker-service
  labels:
    app: blocker-service
spec:
  selector:
    app: blocker-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}