apiVersion: v1
kind: Service
metadata:
  name: cash-service
  labels:
    app: cash-service
spec:
  selector:
    app: cash-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}